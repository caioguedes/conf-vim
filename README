Personal Config For Vim/GVim

Install

	git clone git@github.com:CaioCesarDev/conf-vim.git ~/.vim

Create symlinks:

	ln -s ~/.vim/vimrc ~/.vimrc

Install Plugins

	cd ~/.vim
	git submodule init
	git submodule update
